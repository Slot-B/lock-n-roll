export default function CreatePage() {
  return (
    <div className="mx-auto max-w-3xl px-6 py-12">
      <h1 className="font-display text-4xl tracking-tight">Create Listing</h1>
      <p className="mt-2 text-muted-foreground">
        Tokenize the right to receive your locked tokens — pick a Streamflow
        vesting contract and list it. Coming in M2.
      </p>
    </div>
  );
}
